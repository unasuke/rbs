#!/usr/bin/env ruby

require "stackprof"

out = ENV["RBS_STACKPROF_OUT"] || 'tmp/stackprof-rbs.dump'

StackProf.run(mode: :cpu, out: out, raw: true, interval: 300) do
  load File.join(__dir__, "../exe/rbs")
end
